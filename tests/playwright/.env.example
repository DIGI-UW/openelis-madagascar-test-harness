# Copy to repo root `.env` (playwright.config.ts loads `../.env`) or export before running tests.
# Local distro: proxy serves HTTPS on host port 443 (see docker-compose.yml).

BASE_URL=https://localhost
TEST_USER=admin
TEST_PASS=adminADMIN!
